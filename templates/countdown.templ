package templates

import (
	"fmt"
	"time"
	"toxic-chronos/models"
)

templ Countdown(c models.Countdown) {
	<div style="display: flex; flex-direction: column; justify-content: center; align-items: center;">
		<h2 style="font-size: 3vw; padding: 0px 20px 0px 20px; border-bottom: 3px solid white;">{ c.Label }</h2>
		<div style="font-size: 12vw;" id={ fmt.Sprintf("countdown-%s", c.Label) }>loading ...</div>
	</div>
	<script type="module">
		import { startCountdown } from "/static/live_countdown_display.js";
		startCountdown({{ fmt.Sprintf("countdown-%s", c.Label) }}, {{ c.DueDate.Format(time.RFC3339) }});
	</script>
}
